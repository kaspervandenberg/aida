// © Maastro Clinic, 2014
package nl.maastro.eureca.aida.velocityrdf.n3_id_translator;

import org.junit.Before;
import org.junit.After;
import org.junit.experimental.theories.Theory;
import static org.junit.Assert.*;
import static org.junit.Assume.*;
import static org.hamcrest.Matchers.*;
import checkers.nullness.quals.Nullable;

public abstract class IdentifierSyntaxTest<T> {
/*	private final Class<T> valueType;

	@Nullable
	private Matcher matcher = null;

	protected IdentifierSyntaxTest(Class<T> valueType_)
	{
		this.valueType = valueType_;
	}


	@Before
	public void setup()
	{
		this.matchers = new Matchers(getTestee());
	}


	@After
	public void teardown()
	{
		this.matchers = null;
	}

	
	@After
	public void teardown()
	{
		this.matchers = null;
	}


	@Theory
	public final void testSyntaxErrorNotWellFormed(
			final Identifier.SyntaxError erroneousIdentifier)
	{
		assertThat(erroneousIdentifier.getValue(), 
				not(isWellformedIdentifier()));
	}


	@Theory
	public final void testSyntaxErrorNotMatches(
			final Identifier.SyntaxError erroneousIdentifier,
			final T val)
	{
		assumeThat(val, is(instanceOf(valueType)));
		assertThat(val,
				not(matchesIdentifier(erroneousIdentifier.getValue())));
	}
*/
}

/* vim:set tabstop=4 shiftwidth=4 autoindent textwidth=80 : */

